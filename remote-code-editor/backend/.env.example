# 智谱AI API密钥
ZHIPU_API_KEY=your_api_key_here

# 工作目录（可选，默认为用户主目录下的code-editor-workspace）
WORKSPACE_PATH=

# GitHub访问令牌（可选，用于查看GitHub仓库列表）
# 如何获取：https://github.com/settings/tokens
# 需要权限：repo（访问私有仓库）、read:user（读取用户信息）
# GITHUB_TOKEN=your_github_token_here

# 注意：Git功能使用系统配置的Git身份和凭据
# 请确保服务器已配置Git用户信息：
# git config --global user.name "Your Name"
# git config --global user.email "your.email@example.com"
# git config --global credential.helper store  # 存储凭据以便克隆私有仓库
