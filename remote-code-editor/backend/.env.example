# 智谱AI API密钥（必填）
# 获取方式：https://open.bigmodel.cn/
ZHIPU_API_KEY=your_zhipu_api_key_here

# GitHub访问令牌（用于查看GitHub仓库列表）
# 获取方式：https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# 工作目录（可选，默认为用户主目录下的code-editor-workspace）
# 支持相对路径（相对于项目backend目录）或绝对路径
# 相对路径示例：WORKSPACE_PATH=workspaces/default
# 绝对路径示例：WORKSPACE_PATH=C:/workspace（Windows）或 /home/user/workspace（Linux/Mac）
# 注意：不能设置为项目根目录，应该使用 workspaces 目录下的子目录
WORKSPACE_PATH=workspaces/default
