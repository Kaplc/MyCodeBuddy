# Django核心
Django>=4.2,<5.0

# Django类型存根（用于类型检查）
django-stubs>=4.2
django-stubs-ext>=4.2

# Django REST framework（RESTful API）
djangorestframework>=3.14
djangorestframework-stubs>=3.14

# Django CORS（跨域支持）
django-cors-headers>=4.3

# Django Channels（WebSocket支持）
channels>=4.0
channels_redis>=4.2

# ASGI服务器
daphne>=4.1
uvicorn[standard]>=0.27

# 智谱AI官方SDK
zhipuai>=2.0

# 文件操作
aiofiles>=23.2
pathvalidate>=3.2

# Git操作
GitPython>=3.1

# HTTP请求
requests>=2.31

# 环境变量
python-dotenv>=1.0

# LangGraph (使用本地兼容层替代)
# langgraph>=0.2.0  # 不支持 Python 3.8

# 测试
pytest>=7.4
pytest-django>=4.7
pytest-asyncio>=0.21

# 代码验证架构
mypy>=1.0  # 类型检查
pylint>=3.0  # 静态分析
z3-solver>=4.12  # SMT 约束求解（形式验证）
